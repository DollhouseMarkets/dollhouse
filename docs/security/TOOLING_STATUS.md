# Security tooling status

| Tool | Installed? | Version | Ran? | Result file | Blockers |
|---|---|---|---|---|---|
| Full test suite (`forge test`) | yes (Foundry 1.8.1) | 1.8.1 | yes, completed, in isolation under the shared lock, `FOUNDRY_THREADS=1 forge test --threads 1 -vv --summary`, 3 GB free-RAM floor (coordinator-approved, revised down from an initial 5 GB gate) | `docs/security/full-suite-review-1.txt` | Ran clean: 40 suites, 300 passed, 0 failed, 1 skipped (`SleevePropTest`), wall time 1m44s with a warm build cache. Matches the expected 226 pre-existing + 74 property tests. An earlier attempt with `-q` produced zero stdout at exit 0 in this environment (Foundry's `-q` suppresses log lines only, but nothing else printed either); re-run without `-q` to get real counts. |
| Slither | yes (`uv tool install slither-analyzer --python 3.12`, exposed via `~/.local/bin`) | 0.11.6 | **re-run for review-3** (2026-09-13), in isolation under the shared lock, `FOUNDRY_THREADS=1 slither . --foundry-compile-all --filter-paths "lib/\|test/\|script/\|certora/\|keeper/" --exclude-dependencies --json docs/security/slither-review-3.json`, 3 GB free-RAM floor | `docs/security/slither-review-3-raw.txt` (full text output); `docs/security/slither-review-3.json` (195 contracts, 102 detectors, 272 results, `success: true`); current triage in `docs/security/slither-triage.md`; the prior pass preserved as `docs/security/slither-triage-review-1.md` | Re-run needed because review-3 removed the contestable-purse code (`_board`, the permissionless `rank(uint256)`, `purseWeights`) that the review-1 triage cited by line number. Dispositions were carried over by matching detector, contract and function name (not line number): 160 false positive, 3 accepted risk (unchanged privileged-surface disclosures), 109 needs review (108 carried + 1 new, a `reentrancy-benign` hit on `V4UnlockGuardProbe`, a contract added since review-1). 3 review-1 rows were dropped as purse-code-removed; 1 more (`reentrancy-events` on `FeeVault.accrue`) had no review-3 counterpart for an unverified reason and is flagged for maintainer hand-triage rather than silently dropped. This run's via-IR compile peaked around 8 GB resident (higher than review-1's 4-7 GB, likely reflecting current system load), free system RAM briefly dropped under 500 MB, and it completed without a crash. The same unrelated exit code 127 from a trailing post-analysis step appears after the JSON was already written and validated, matching the review-1 pattern. |
| Halmos | yes (`uv tool install halmos --python 3.12`) | 0.3.3 | **yes, completed with real verdicts** (review-1, 2026-09-12). 9 symbolic checks over the pure libraries: **9 passed, 0 failed, 77.7 s wall**, no counterexamples. Run in isolation under the shared lock with the lean `[profile.halmos]`: `FOUNDRY_PROFILE=halmos FOUNDRY_THREADS=1 halmos --root . --forge-build-out out-halmos --contract '^(CurveMathCheck|FenwickCheck)$' --solver-timeout-assertion 120000 --solver-threads 1 --statistics --no-status`. Covers `CurveMath.floorToSpacing` at the production tick spacing 60 plus 1/10/200, the `sqrtPriceAtFdv` range guard, and `FenwickRangeAdd` properties SLV-01 / SLV-02 / SLV-04 bounded to `M <= 8`. | `docs/security/halmos-review-1.md` (earlier failed attempt: `docs/security/halmos-curvemath.txt`) | **Resolved.** The earlier inconclusive run had two tooling causes, both fixed: (1) the `[profile.halmos]` `skip` globs matched nothing useful -- forge's skip matcher lets `*` cross a path separator, so `"contracts/*.sol"` also swallowed `contracts/libraries/` and `"test/*.t.sol"` swallowed the halmos test itself, so the profile compiled ZERO files; the list is now explicit per-file. (2) halmos parses `--forge-build-out`, which DEFAULTS TO `out` -- it was analysing the full via-IR project artifacts regardless of `FOUNDRY_PROFILE`, which is what caused the ~7 GB compile and the Z3 access violation. Passing `--forge-build-out out-halmos` is mandatory; without it the profile is cosmetic. The profile also now has its own `cache_path = "cache-halmos"`. Peak memory now: lean build ~2 s with no measurable dip (free RAM floor 4.24 GB), halmos run minimum free RAM 9.39 GB. Remaining limits, all documented in the review file: `via_ir = false`, so these are library SEMANTICS checks, not checks of the shipped IR bytecode; the FDV round-trip and the symbolic-tick-spacing floor check are genuine TIMEOUTs (`FullMath.mulDiv` + `Math.sqrt`, and 256-bit division by a symbolic divisor) and are parked in `test/halmos/CurveMathSlowCheck.t.sol`, excluded from the default run; round-schedule and snipe-tax math are not pure library functions (they live on `RoundManager`/`FamilyHook` state) so they are out of Halmos's reach here. |
| Medusa | yes (native Windows binary, `github.com/crytic/medusa` release v1.5.1, extracted to `~/.local/bin/medusa.exe`) | 1.5.1 | **yes, completed and actually fuzzing** (second pass). A Medusa-native harness, `test/medusa/MedusaTarget.sol`, now deploys the whole stack from its own constructor with no cheatcodes, so Medusa has a real target. Run in isolation under the shared lock: `FOUNDRY_PROFILE=medusa FOUNDRY_THREADS=1 medusa fuzz --config scripts/security/medusa.json` | `docs/security/medusa-review-1.txt` (tail of the run); config `scripts/security/medusa.json`; harness `test/medusa/MedusaTarget.sol` + sanity check `test/medusa/MedusaTargetSanity.sol` | **Targets found: 1** (`MedusaTarget`), 42 tests: 37 assertion tests (every public method) + 5 property tests. **Calls: 164,896 in 300s = ~565 calls/s** (~5 sequences/s, ~112M gas/s), 6,529 branches, corpus 121 sequences, wall time 413s including the crytic-compile step. **Properties: 5/5 held, 0 failed** — FEE-11 (vault solvency), FEE-08 (family ledgers are hop fees only), SUP-01 (fixed supply), RND-09 (write-once canonical history), BID-05 (keeper leash). No failing sequence, so `docs/security/medusa-failures.txt` was not created. **Coverage: 83.4% of the harness's own lines; 9.7% project-wide as lcov reports it, with ~0% against `contracts/` — this number is a reporting artifact, not the real reach.** Medusa maps coverage only onto contracts whose bytecode it can match to a compilation artifact; everything here (PoolManager, factory, hook, vault, RoundManager, tokens) is deployed at RUNTIME by the harness constructor under `bytecode_hash = "none"` + via-IR, so it goes unattributed. That the protocol really is being driven is proved by `test/medusa/MedusaTargetSanity.sol` (1 test, passing): the constructor deploys the stack, a buy succeeds, a candidate registers, a round finalizes, and all 5 properties hold. The earlier "no fuzzable targets" result is superseded. |
| Foundry deep fuzz/invariant | yes (Foundry 1.8.1 already present) | 1.8.1 | smoke run: `FOUNDRY_FUZZ_RUNS=2000 FOUNDRY_INVARIANT_RUNS=100 FOUNDRY_INVARIANT_DEPTH=100 forge test --match-contract Invariants` — see `docs/security/fuzz-smoke.txt`. **Deep pass completed**, in isolation under the shared lock, single-threaded: `FOUNDRY_THREADS=1 FOUNDRY_FUZZ_RUNS=5000 FOUNDRY_INVARIANT_RUNS=200 FOUNDRY_INVARIANT_DEPTH=100 forge test --threads 1 --match-path 'test/properties/*'`, 3 GB free-RAM floor (8.9 GB free at start) | `docs/security/fuzz-deep-review-1.txt` (tail of run), `docs/security/fuzz-deep-review-1-full.txt` (full output) | Smoke run passed: 1/1 test, all 5 `invariant_*` checks green, runs=100 calls=10000 reverts=16 (all in `forceSuccession`, expected — random walk rarely clears the succession threshold on its own). Deep pass against the full `test/properties/*` suite ran clean: 13 test suites, 74 passed, 0 failed, 1 skipped (`SleevePropTest`, same skip as the regular full-suite run), 75 total tests, wall time 704s (~11.7 min). No failures, so no counterexample capture was needed. No forge/solc processes remained after exit; lock directory released. |

## Environment inventory

- WSL: `docker-desktop` distribution present (state: Stopped) but no general-purpose Linux
  distro (e.g. Ubuntu) installed; `wsl --list --online` shows Ubuntu as installable but it is
  not present. Not needed in practice — every tool above runs natively on Windows.
- Docker: Docker Desktop 29.3.1 installed.
- Python: `py -0` shows 3.14 (default) and 3.13 installed via the `py` launcher; neither is
  used directly — `uv` provisioned its own Python 3.12 for all three tool installs.
- `uv`: not preinstalled; installed via `py -3.13 -m pip install --user uv`, landing at
  `~/AppData/Roaming/Python/Python313/Scripts/uv.exe`. All three security tools (plus
  `crytic-compile`, a Medusa dependency needed as its own console script) were installed with
  `uv tool install <pkg> --python 3.12`, landing in `~/.local/bin`.
- `pipx`, `go`: not installed; not needed since `uv` covered every tool here.
- Foundry: 1.8.1, already present at `~/.foundry/bin`.

## Outstanding work (deferred, not attempted further this session)

- ~~Re-run Slither and the Halmos smoke check **in isolation** (no concurrent `forge`/`solc`
  processes) to get past the `std::bad_alloc` failures~~ Done for both; see the Slither and Halmos
  rows above and `docs/security/halmos-review-1.md`.
- Halmos follow-ups that need a `contracts/` change, so deliberately NOT done: extract the round
  schedule (`D(n)`, `lateEntryUntil`, `bondFor`) and the snipe-tax ramp into pure libraries so they
  become symbolically reachable without deploying `RoundManager`/`FamilyHook`.
- Once `docs/security/slither.json` exists, fill in the real detector table in
  `docs/security/slither-triage.md`.
- ~~Run the full deep fuzz/invariant pass via `scripts/security/fuzz-deep.sh` when the machine
  is not under concurrent forge load.~~ Done — see the Foundry deep fuzz/invariant row above
  (74 passed, 0 failed, 1 skipped, 704s wall time).
